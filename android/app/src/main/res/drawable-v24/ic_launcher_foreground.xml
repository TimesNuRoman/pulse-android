<?xml version="1.0" encoding="utf-8"?>
<!--
  R246 - Pre-API-26 launcher-icon foreground.
  Sync to the paper-plane mark used on Android 8+ (R244 EmptyState
  design). Android 7 users were still seeing the P+spark from R166;
  this update keeps the brand consistent across all supported OS
  versions. Same geometry as drawable/ic_launcher_foreground.xml.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">

    <!-- Upper wing (cyan, the main brand color). -->
    <path
        android:fillColor="#7aa2f7"
        android:pathData="M30,54 L78,34 L52,58 Z" />

    <!-- Lower wing (Tokyo Night dark blue, suggests the fold). -->
    <path
        android:fillColor="#3d59a1"
        android:pathData="M30,54 L52,58 L64,74 Z" />
</vector>
